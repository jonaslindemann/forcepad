PROJECT(forcepad)

SET(FLTK_ROOT "/Users/jonas/Libraries/fltk-1.1.9")
SET(GLEW_ROOT "/Users/jonas/Libraries/glew")

ADD_DEFINITIONS(-D_CRT_SECURE_NO_WARNINGS)

INCLUDE_DIRECTORIES(${FLTK_ROOT} ${FLTK_ROOT}/jpeg ${FLTK_ROOT}/png ${FLTK_ROOT}/zlib ${GLEW_ROOT}/include src/calfemcxx src/newmat src/defs)
LINK_DIRECTORIES(${FLTK_ROOT}/lib ${PROJECT_SOURCE_DIR}/lib ${GLEW_ROOT}/lib)
#LINK_DIRECTORIES(${PROJECT_SOURCE_DIR}/../fltk-1.1.8/build_xcode/bin ${PROJECT_SOURCE_DIR}/../calfemcxx-1.0/lib ${PROJECT_SOURCE_DIR}/../newmat-10.A/lib)

SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)

IF(WIN32)
	SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)
ELSE(WIN32)
	SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin/release)
ENDIF(WIN32)

SUBDIRS(src)
