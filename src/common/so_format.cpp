#include "so_format.h"

/*
static char g_buff[512];

#include <cstdarg>
#include <cstdio>

const char* so_format(const std::string &format, ...)
{
	va_list args; 
    va_start(args, format.c_str());
    vsprintf (g_buff, format.c_str(), args);

	return g_buff;
}
*/
