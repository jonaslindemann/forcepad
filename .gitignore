
*.exe
*.ilk
*.pdb
*.am
*.in
# ...but keep the MSIX package manifest template (configured by CMake)
!src/qtforcepad/AppxManifest.xml.in
*.manifest
# ...but keep the app manifest that declares DPI awareness (linked into the exe)
!src/qtforcepad/qtforcepad.manifest
*.dll
build/
build-release/
build-debug/
build-wasm/
bin/wasm/
*.lib
/Win32
/Win32/Debug
win32_build_release/
win32_build_debug/
vcpkg_installed/
*.ps1
# ...but keep the wasm build/serve helper scripts (tooling referenced by docs)
!scripts/wasm-build.ps1
!scripts/wasm-serve.ps1
# ...and the UML compilation-database helper (used by scripts/gen-uml.cmd)
!scripts/msvc-db-to-clang.ps1
# ...and the MSIX development-certificate helper (used by qtforcepad_msix)
!scripts/msix-devcert.ps1
/docs/site
# UML diagrams (scripts/gen-uml.cmd): configure-only CMake tree for the
# compilation database, the downloaded PlantUML jar, and the .puml
# intermediates. The rendered .svg/.png are left tracked so they can be used
# in the docs site.
/build-uml/
/tools/plantuml.jar
/docs/uml/*.puml
/docs/uml/*.plantuml
bin/release/qtforcepad
/bin/release/qtforcepad.app
bin/.DS_Store
bin/release/.DS_Store
bin/release/qtforcepadd
/lib
/build-wasm-rel
.DS_Store
/bin/release/ForcePAD.app
*.dmg
